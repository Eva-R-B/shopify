{% comment %}
    This section displays all collections in a responsive grid.
{% endcomment %}

<style>
    .background-color-on-section{
        padding-bottom: 3rem;
    }
.all-collections-grid .category-heading {
  color: var(--accent-color);
  padding: 2rem 0 1.5rem;
  margin: 0;
  text-align: center;
  font-size: 2.5rem;
  font-weight: 300;
}
.all-collections-grid .grid {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1rem;
}
.grid__item {
    text-align: center;
    background: #fff;
    border-radius: 8px;
    padding: 1rem;
    transition: box-shadow 0.2s;
}

.collection-image {
    width: 250px;
    height: 190px;
    object-fit: cover;
    border-radius: 6px;
    margin-bottom: 0.75rem;
}
.collection-placeholder {
    width: 100%;
    height: 180px;
    background: #f2f2f2;
    border-radius: 6px;
    margin-bottom: 0.75rem;
}
.collection-title {
    font-size: 1.1rem;
    font-weight: 600;
    color: #222;
    margin: 0;
}
.collection-link {
    text-decoration: none;
    color: inherit;
    display: block;
}

</style>
<div class="background-color-on-section">
<div class="all-collections-grid">
      <h1 class="category-heading">Shop efter kategorier</h1>
    <div class="grid">
        {% for collection in collections %} 
            <div class="grid__item">
                <a href="{{ collection.url }}" class="collection-link">
                    {% if collection.image %}
                        <img src="{{ collection.image | img_url: '480x480' }}" alt="{{ collection.title | escape }}" class="collection-image" />
                    {% else %}
                        <div class="collection-placeholder"></div>
                    {% endif %}
                    <h3 class="collection-title">{{ collection.title }}</h3>
                </a>
            </div>
        {% endfor %}
    </div>
</div>
</div>

{% schema %}
{
  "name": "All Collections Grid",
  "tag": "section",
  "settings": [],
  "presets": [
    {
      "name": "All Collections"
    }
  ]
}
{% endschema %}
