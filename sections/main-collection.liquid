<style>

.collection-section {
  margin-bottom: 5rem;
}

.category-heading {
  color: var(--accent-color);
  margin: 2rem 0 1.5rem;
  text-align: center;
  font-size: 2rem;
  font-weight: 400;
}

.category-divider {
  border: none;
  border-top: 1px solid #ececec;
  margin: 2.5rem auto;
}

.collection-products {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 1.5rem;
  list-style: none;
  padding: 0;
}

.product-card {
  background: #fff;
  border: 1px solid #eee;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
  overflow: hidden;
  transition: box-shadow 0.2s, border-color 0.2s;
  transition: transform 0.3s ease;
}

.product-card:hover {
  transform: scale(1.02);
}

.product-card img {
  width: 100%;
  height: 200px;
  object-fit: cover;
  border-bottom: none;
  display: block;
}

.product-card h2 {
  font-size: 1.2rem;
  margin: 16px 12px 8px 12px;
  color: #222;
  font-weight: 500;
}

.product-card p {
  margin: 0 12px 16px 12px;
  color: var(--accent-color);
  font-size: 1.1rem;
  font-weight: 500;
}

.product-card a {
  text-decoration: none;
  color: inherit;
  display: block;
}
</style>

<!-- Collection Page . or called Catalog in shopify. 
WE DON´T USE THIS ANYMORE BUT THERE IS A POSSIBILITY for displaying products after kategories. title+ products. -->
{% comment %} collection with all products {% endcomment %}
{% comment %} <div class="container"> {% endcomment %}

<!-- Kvinde kategori -->
  {% comment %} <section class="collection-section">
    <h2 class="category-heading">Kvinde kategori</h2>
    <ul class="collection-products">
      {% for product in collections['kvinde-kategori'].products %}
        <li class="product-card">
          <a href="{{ product.url }}">
            <img src="{{ product.featured_image | img_url: 'medium' }}" alt="{{ product.title }}">
            <h2>{{ product.title }}</h2>
            <p>{{ product.price | money }}</p>
          </a>
        </li> {% endcomment %}

  {% comment %} <!-- Mænd kategori -->
  <section class="collection-section">
    <h2 class="category-heading">Mænd kategori produkter</h2>
    <ul class="collection-products">
      {% for product in collections['maend-kategori'].products %}
        <li class="product-card">
          <a href="{{ product.url }}">
            <img src="{{ product.featured_image | img_url: 'medium' }}" alt="{{ product.title }}">
            <h2>{{ product.title }}</h2>
            <p>{{ product.price | money }}</p>
          </a>
        </li>
      {% endfor %}
    </ul>
    <hr class="category-divider">
  </section>

  <!-- Under 1000 DKK kategori -->
  <section class="collection-section">
    <h2 class="category-heading">Under 1000 DKK produkter</h2>
    <ul class="collection-products">
      {% for product in collections['under-1000-dkk'].products %}
        <li class="product-card">
          <a href="{{ product.url }}">
            <img src="{{ product.featured_image | img_url: 'medium' }}" alt="{{ product.title }}">
            <h2>{{ product.title }}</h2>
            <p>{{ product.price | money }}</p>
          </a>
        </li>
      {% endfor %}
    </ul>
  </section>

</div> {% endcomment %}
